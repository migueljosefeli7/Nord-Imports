import{NextResponse}from"next/server";import{products}from"@/lib/data";export async function GET(req:Request){const q=new URL(req.url).searchParams.get("q")?.trim().toLowerCase()||"";if(q.length<2)return NextResponse.json([]);return NextResponse.json(products.filter(p=>p.active&&[p.name,p.brand,p.category,p.description].join(" ").toLowerCase().includes(q)).slice(0,12).map(({id,slug,name,brand,category,image})=>({id,slug,name,brand,category,image})))}
