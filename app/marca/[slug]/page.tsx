import{notFound}from"next/navigation";import{brands,products}from"@/lib/data";import{StoreHeader}from"@/components/store-header";import{StoreFooter}from"@/components/store-footer";import{ProductCard}from"@/components/product-card";export function generateStaticParams(){return brands.map(b=>({slug:b.toLowerCase().replaceAll(" ","-")}))}export default async function Brand({params}:{params:Promise<{slug:string}>}){const{slug}=await params,brand=brands.find(b=>b.toLowerCase().replaceAll(" ","-")===slug);if(!brand)notFound();const list=products.filter(p=>p.brand===brand),cats=[...new Set(list.map(p=>p.category))];return <><StoreHeader/><main className="catalog"><p className="eyebrow">MARCA</p><h1 className="brand-title">{brand}</h1><p style={{margin:"25px 0 50px",color:"#777"}}>Categorias: {cats.join(" · ")}</p><div className="products-grid">{list.map((p,i)=><ProductCard key={p.id} product={p} index={i}/>)}</div></main><StoreFooter/></>}
