"use client";import Link from"next/link";import{useState}from"react";import{supabaseBrowser}from"@/lib/supabase";export function AuthForm({mode}:{mode:"login"|"signup"}){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setMessage("Enviando...");try{const s=supabaseBrowser();const{error}=mode==="login"?await s.auth.signInWithPassword({email,password}):await s.auth.signUp({email,password,options:{emailRedirectTo:`${location.origin}/auth/callback`}});if(error)throw error;if(mode==="signup")location.href="/auth/sucesso";else location.href="/admin"}catch(e){setMessage(e instanceof Error?e.message:"Não foi possível continuar.")}}return <main className="auth-page"><Link href="/" className="wordmark"><span>N</span>NORD <b>IMPORTS</b></Link><form onSubmit={submit} className="auth-card"><p className="eyebrow">ÁREA RESTRITA</p><h1>{mode==="login"?"Acessar painel":"Criar conta"}</h1><label>E-mail<input type="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input type="password" minLength={6} required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button primary">{mode==="login"?"ENTRAR":"CADASTRAR"}</button>{message&&<p>{message}</p>}<Link href={mode==="login"?"/cadastro":"/login"}>{mode==="login"?"Ainda não tenho uma conta":"Já tenho uma conta"}</Link></form></main>}
