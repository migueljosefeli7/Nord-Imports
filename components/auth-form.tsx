"use client";import Link from"next/link";import{useState}from"react";import{BrandLogo}from"@/components/brand-logo";import{supabaseBrowser}from"@/lib/supabase";export function AuthForm({mode}:{mode:"login"|"signup"}){const[email,setEmail]=useState(""),[password,setPassword]=useState(""),[message,setMessage]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setMessage("Enviando...");try{const s=supabaseBrowser();const{error}=mode==="login"?await s.auth.signInWithPassword({email,password}):await s.auth.signUp({email,password,options:{emailRedirectTo:`${location.origin}/auth/callback`}});if(error)throw error;if(mode==="signup")location.href="/auth/sucesso";else location.href="/admin"}catch(e){setMessage(e instanceof Error?e.message:"Não foi possível continuar.")}}return <main className="auth-page"><Link href="/" aria-label="Voltar para a Nord Imports"><BrandLogo className="header-logo"/></Link><form onSubmit={submit} className="auth-card"><p className="eyebrow">ÁREA RESTRITA</p><h1>{mode==="login"?"Acessar painel":"Criar conta"}</h1><label>E-mail<input type="email" autoComplete="email" required value={email} onChange={e=>setEmail(e.target.value)}/></label><label>Senha<input type="password" autoComplete={mode==="login"?"current-password":"new-password"} minLength={6} required value={password} onChange={e=>setPassword(e.target.value)}/></label><button className="button primary">{mode==="login"?"ENTRAR":"CADASTRAR"}</button>{message&&<p aria-live="polite">{message}</p>}<Link href={mode==="login"?"/cadastro":"/login"}>{mode==="login"?"Ainda não tenho uma conta":"Já tenho uma conta"}</Link></form></main>}
