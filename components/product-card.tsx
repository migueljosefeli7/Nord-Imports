import Link from "next/link";import{ArrowUpRight}from"lucide-react";import type{Product}from"@/lib/data";
export function ProductCard({product,index=0}:{product:Product,index?:number}){return <article className="product-card"><Link href={`/produto/${product.slug}`} className="product-image"><img src={product.image} alt={product.name}/><span className="product-number">{String(index+1).padStart(2,"0")}</span><span className="view-tag">VER PEÇA <ArrowUpRight size={14}/></span></Link><div className="product-info"><div><p>{product.brand}</p><h3><Link href={`/produto/${product.slug}`}>{product.name}</Link></h3></div><span>Sob consulta</span></div></article>}
