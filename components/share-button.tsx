"use client";import{Share2}from"lucide-react";import{useState}from"react";export function ShareButton(){const[copied,setCopied]=useState(false);async function share(){if(navigator.share)await navigator.share({title:document.title,url:location.href});else{await navigator.clipboard.writeText(location.href);setCopied(true);setTimeout(()=>setCopied(false),1800)}}return <button className="button ghost" style={{color:"#090b10",borderColor:"#090b10"}} onClick={share}><Share2/>{copied?"LINK COPIADO":"COMPARTILHAR"}</button>}
